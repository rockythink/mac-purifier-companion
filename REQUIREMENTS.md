# Mac 净化器伴侣

免费开源的原生 macOS 菜单栏工具：监测 Mac 状态，手动调节小米净化器风量，或按 CPU 温度自动联动。

- 源码：<https://github.com/rockythink/mac-purifier-companion>
- 下载：<https://github.com/rockythink/mac-purifier-companion/releases>
- 问题反馈：<https://github.com/rockythink/mac-purifier-companion/issues>

官方发行包免费下载，项目原创代码按 **GPL-3.0-only** 发布，不附加非商业限制。GPL 允许使用、修改、再分发及商业使用；分发修改版本时须遵守对应源码与许可证义务。本软件不提供任何担保。第三方组件、图片和商标的权利另见下文。

## 平台与兼容范围

构建目标为 **Apple Silicon / macOS 14+**，Swift 工具链要求 6.3；不支持 Intel Mac。目前仅在 **Apple M4 / macOS 27** 上做过实机验证，不能据此承诺所有系统和机型兼容。

目前完成实机连接与控制验证的净化器只有 **小米空气净化器 2，`zhimi.airpurifier.m1`**。已实测状态读取、RPM 回读，以及最爱等级 5、6、8 的切换与恢复。手动界面开放 SDK 支持的 **0–17 整数档位**，但未逐档进行实体测试。0 是最低最爱档，不是关机；档位不对应固定 RPM。

锁定的 `python-miio 0.5.12` 还识别以下旧版 miIO 协议候选型号：`zhimi.airpurifier.` 加 `v1`、`v2`、`v3`、`v5`、`v6`、`v7`、`m1`、`m2`、`ma1`、`ma2`、`sa1`、`sa2`、`mc1`、`mc2`。这是 SDK 候选范围，不是实机兼容承诺。其他米家净化器、新协议设备及多设备同时控制不在当前支持范围内。

## 下载与安装

当前版本为 **1.5.1（构建 9）**，发行文件为：

- `Mac-Purifier-Companion-1.5.1-arm64.dmg`：安装镜像。
- `Mac-Purifier-Companion-1.5.1-source.zip`：本项目及锁定依赖的对应源码和许可材料。
- `SHA256SUMS`：上述两个文件的校验和。

1. 从本项目 Releases 页面下载安装镜像，签名、公证及验证结果以该版本的 Release 说明为准。
2. 将镜像中的 `MacFanLink.app` 拖到“应用程序”，再从“应用程序”启动。
3. 应用显示名为 **Mac 净化器伴侣**。技术文件名、Bundle ID 和安装路径仍保留 `MacFanLink`，避免已有配对、钥匙串、偏好及辅助程序授权迁移。
4. 不配对净化器也能使用 Mac 本机监测与历史趋势。不要为了安装关闭 SIP 或绕过系统安全保护。

下载后可将三个文件放在同一目录，执行 `shasum -a 256 -c SHA256SUMS`。遇到系统完整性或开发者警告时，先检查来源、版本和 Release 说明，不要默认绕过。

## 功能

- 菜单栏展示 CPU 温度或净化器实测 RPM；弹窗包含温度、负载、内存、Swap、热状态、进程摘要与四组趋势。
- 本机状态、历史趋势、净化器设备、账号与运行设置分为独立页面。
- 净化器支持显式手动风量与基于 CPU 平均温度的自动联动。
- “安静优先”“均衡”“散热优先”模板可继续编辑四个温度阈值、升降档确认时间、最短调节间隔及中高档位。
- 个人预设支持命名另存、载入、更新、重命名及确认删除，重启后保留。
- 可选 Mac 内置风扇高级手动控制；每个检测到的风扇独立控制，默认由 macOS 自动管理。

## 首次配对与启用

1. 将 Mac 与净化器放在可互访的同一局域网，确认净化器已经加入米家账号。
2. 打开“米家账号”，选择与米家 App 一致的地区，使用本人米家 App 扫码并确认授权，再选择净化器。登录与选择本身不调档。
3. 等待局域网核验型号、开关机状态及设备回读。不要在型号不匹配或读取异常时继续控制。
4. 保持联动暂停或停止，在“联动规则”设置中档、高档及温度条件，再选择“试听两档”。试听会真实写入，每档约 10 秒，并在控制权未被人工改变时尝试恢复原状态。
5. 确认风量、噪声及恢复结果后，点击“应用到联动”。应用规则不会自动启用联动，最后须由使用者明确启用。

自动联动使用的档位必须经过试听验证。手动选中过某个档位不会自动把它标为已验证。

“重新选择设备”会重新扫码。“退出并切换账号”只删除本机授权，不从米家云端解绑设备；若旧设备恢复未决，会拒绝切换。

## 草稿、预设与实际控制

**载入模板、编辑参数、保存个人预设都不发送设备命令。** 联动启用、持有控制权或操作尚未完成时，禁止应用规则；请先暂停或停止。应用规则本身不会启用联动。

自动联动依据 CPU 平均温度、中高两档、连续确认时间、回差和最短调节间隔进行控制。短暂低温不会立即退出：例如退出条件要求连续低于阈值，期间再次超过阈值会重新计时。

手动页在自动模式下拖动净化器滑杆只改变草稿，显式选择“手动风速”才进入手动控制。手动模式下拖动结束会应用档位，也可使用显式应用按钮。显示的 RPM 始终来自设备回读，不由档位换算。离开页面不会自动结束手动控制。

Mac 风扇手动控制需要使用者安装并批准受限辅助程序。安装助手不会自动调速；系统授权由使用者本人完成。本项目没有为 Mac 内部风扇新增温度调速规则，固定 RPM 可能降低动态散热余量。

## 停止、恢复与安全

- 不自动开启原本关闭的净化器；读取过期、离线、恢复未决或外部人工修改时会停止写入或暂停。
- 暂停、停止及正常退出时，依据接管前快照和当前控制权尝试恢复。检测到米家 App、实体按键或其他程序修改设备后，不强行覆盖。
- 以设备回读为准，“已发送命令”不等于硬件已恢复。恢复未确认时，先检查本应用或米家 App 中的设备状态。
- 断电、强制结束、系统崩溃或网络中断可能使净化器停留在最后档位，不能保证恢复。
- 空气净化器不会制冷，项目不保证降温、性能提升或延长硬件寿命。CPU/GPU 芯片温度也不是机身温度或室温。
- 不要遮挡任一设备的进出风口，不建议把 Mac 放在净化器出风格栅上。界面叠放示意不是安全摆放建议。
- 策略阈值不是 Apple 安全温度标准；真实休眠唤醒、突然断电、所有候选设备、地区及档位尚未完整实测。

## 隐私与网络访问

- Mac 指标、进程摘要、历史趋势与联动事件保存在本机，历史默认保留 30 天。非敏感状态位于 `~/Library/Application Support/cc.ss-data.MacFanLink/`；个人预设位于本机应用偏好。
- 净化器 token 保存在 macOS 钥匙串，不写入普通配置文件或仓库。扫码云会话不持久化，临时二维码在完成、取消或过期后清理。
- 扫码与设备列表需要访问小米 HTTPS 服务；配对后的控制通过局域网直连设备。产品图片会按需访问目录中记录的小米图片地址。
- 没有项目自建的账号服务器或遥测上传功能。通知默认关闭，启用后才发送。
- `Resources/vendor/token_extractor.py` 是固定版本的上游依赖，不是受支持的独立登录入口。不要直接以 DEBUG 日志运行它；上游调试输出可能包含认证数据。
- 反馈问题时，不要上传 token、二维码、完整认证 URL、钥匙串、设备身份或未经脱敏的本机数据目录。

## 从源码构建

需要 Apple Silicon Mac、Xcode 的 Swift 6.3 工具链、`uv`、Python 3.12.12，以及 arm64 的 `macmon 0.8.2`。首次构建需联网下载锁定依赖和对应源码。

```bash
git clone https://github.com/rockythink/mac-purifier-companion.git
cd mac-purifier-companion
uv python install 3.12.12
uv sync --frozen --python 3.12.12

uv run --frozen python -B -m unittest discover -s tests
/usr/bin/xcrun swift test
```

确保 `macmon --version` 为 0.8.2。若未加入 PATH，通过 `MACFANLINK_MACMON_SOURCE` 指定二进制路径。Python 环境元数据的版本是内部运行环境版本，不代表应用发行版本。

仅供本机使用的临时签名构建：

```bash
MACFANLINK_SIGN_IDENTITY=- bash scripts/build_app.sh
```

产物为 `dist/MacFanLink.app`。临时签名不等于 Developer ID 或公证；受签名与系统授权保护的风扇助手不能据此保证可用。

构建安装镜像、对应源码及校验文件：

```bash
MACFANLINK_SIGN_IDENTITY=- bash scripts/package_distribution.sh
cd dist
shasum -a 256 -c SHA256SUMS
```

正式发行时使用维护者自己的 Developer ID Application 身份，通过 `MACFANLINK_SIGN_IDENTITY` 指定；脚本也能发现唯一可用身份。版本与构建号可用 `MACFANLINK_VERSION`、`MACFANLINK_BUILD_NUMBER` 显式覆盖。正式发行还须另行公证、装订票据、检查 Gatekeeper、实际安装运行，并在装订后重新生成校验和。不要提交开发者证书、私钥、本机状态或构建目录。

## 许可证与第三方权利

项目原创代码 Copyright (c) 2026 Elazer，许可证全文见 [LICENSE](LICENSE)。第三方来源、版权及许可见 [Resources/ThirdPartyNotices.txt](Resources/ThirdPartyNotices.txt)。

发行包使用 `python-miio 0.5.12`、`macmon 0.8.2`、固定版本 Xiaomi-cloud-tokens-extractor、AppleSMC ABI 参考实现、CPython、python-build-standalone 和锁定 Python 依赖。对应源码 ZIP 包含相关来源归档及许可材料，分发二进制时应同时提供。

本项目不是 Apple、小米或米家官方产品，不暗示官方认可。产品名称、商标与远程产品图片不属于项目 GPL 授权范围；图片按原始公开目录链接远程加载，不打包进安装文件或源码包，也不主张额外的图片使用许可。目录中的公共图片访问参数来自该目录，并非用户账号凭据。
